<div class="d-grid" style="margin-left: 5px">
    @if($disabled)
        <button type="button" class="ml-1 btn btn-primary btn-lg disabled">
            <i class="ti ti-device-floppy me-2"></i>
            Save
        </button>
    @else
        <button type="submit" class="ml-1 btn btn-primary btn-lg">
            <i class="ti ti-device-floppy me-2"></i>
            Save
        </button>
    @endif
</div>
